{
  "name": "cfp-hono-proxy",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "wrangler pages dev",
    "deploy": "npm run build && wrangler pages deploy",
    "biome:format": "biome format --fix ."
  },
  "dependencies": {
    "hono": "^4.12.9"
  },
  "devDependencies": {
    "@atolycs/biome-config": "^0.3.2",
    "@cloudflare/workers-types": "^4.20260317.1",
    "@hono/vite-build": "^1.10.1",
    "@hono/vite-dev-server": "^0.19.1",
    "vite": "^6.4.1",
    "wrangler": "^4.77.0"
  }
}
